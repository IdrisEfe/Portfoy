"use client";
import { PageHeading } from "./page-heading";
import { DigitalMask } from "./digital-mask";
import { siteContent } from "@/lib/content";
import { useSite } from "./site-provider";
export function AboutExperience() { const { locale } = useSite(); return <div className="page-wrap"><PageHeading index="06" eyebrow={{ en: "SELF-TAUGHT / ALWAYS IN MOTION", tr: "KENDİ KENDİNE / DAİMA HAREKETTE" }} title={siteContent.identity.name} body={{ en: "An ambitious builder interested in useful software, unusual interfaces, and learning quickly enough to turn possibility into something real.", tr: "Faydalı yazılımlar, sıra dışı arayüzler ve olasılıkları gerçeğe dönüştürecek hızda öğrenmekle ilgilenen hırslı bir üretici." }} /><div className="about-grid"><DigitalMask compact mode={siteContent.avatar.mode} /><div><h2>{locale === "en" ? "The short version" : "Kısa hâli"}</h2><p>{locale === "en" ? "I build by asking better questions, reducing ideas to working experiments, and following the strongest signal. This page grows into a full bilingual résumé through the owner dashboard." : "Daha iyi sorular sorarak, fikirleri çalışan deneylere indirgeyerek ve en güçlü sinyali takip ederek üretiyorum. Bu sayfa yönetim paneli üzerinden iki dilli bir özgeçmişe dönüşüyor."}</p>{siteContent.resume.visible && siteContent.resume.url ? <a className="secondary-button" href={siteContent.resume.url} target="_blank" rel="noreferrer">{locale === "en" ? "Download résumé" : "Özgeçmişi indir"}</a> : <span className="secondary-button disabled" aria-disabled="true">{locale === "en" ? "Résumé coming soon" : "Özgeçmiş yakında"}</span>}</div></div></div>; }
